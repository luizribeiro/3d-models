// Model purpose: HSW holder for a Klein tool/accessory, modeled as a dedicated wall-mounted pocket/cradle for repeatable organization.
// Keeps the tool visible and near point-of-use while staying compatible with your HSW layout.
include <lib/hsw.scad>

hsw_box(
  inner_width = 65.5,
  inner_height = 24,
  inner_depth = 40,
  thickness = 3,
  corner_radius = 2
);
